# Changelog

## 0.1.0 (unreleased)

First stable release after the `0.1.0-alpha.0` line.

- Wired by `npx webdev init` (vite + storybook detection, byte-perfect text-insert into `plugins:[]` and `addons:[]`).
- TanStack Start dogfood example.

## 0.1.0-alpha.0

Initial alpha. Vite plugin + Storybook preset for webdev.
