# Changelog

## 0.1.0 (unreleased)

Initial release. Astro integration for webdev — adds the gateway client to your Astro dev server via `astro.config.{mjs,ts,js}`'s `integrations:[]`. Wired by `npx webdev init`.
