# web-dev-mcp Gateway Architecture

## Entity Model

```mermaid
erDiagram
    GATEWAY ||--o{ PROJECT : "tracks"
    GATEWAY ||--o{ MCP_SESSION : "hosts"
    GATEWAY ||--o| CDP_RELAY : "owns"
    PROJECT ||--|{ SERVER : "has instances"
    SERVER ||--o{ BROWSER : "connected to"
    CDP_RELAY ||--o{ AVAILABLE_TAB : "knows about"
    CDP_RELAY ||--o{ PLAYWRIGHT_CLIENT : "serves"

    GATEWAY {
        string serverUrl "http://localhost:3333"
        string sessionId "sha256(serverUrl)[0:6]"
        string logDir ".web-dev-mcp/"
        number startedAt "Date.now()"
        number checkpointTs "null | timestamp"
        string mode "hub | hybrid"
    }

    PROJECT {
        string projectId "basename-hash4 (e.g. vite-app-a3f7)"
        string directory "absolute path (identity)"
        string logDir "directory/.web-dev-mcp/"
    }

    SERVER {
        string id "PID string (ephemeral)"
        string projectId "FK to Project"
        string directory "FK to Project"
        string type "vite | nextjs | storybook | generic"
        number port "dev server port"
        number pid "process ID"
        string name "optional friendly name"
        string rpcEndpoint "ws://localhost:PORT/__rpc"
        string mcpEndpoint "http://localhost:PORT/__mcp/sse"
        object logPaths "channel -> file path"
        number registeredAt "timestamp"
    }

    BROWSER {
        string connId "random uid (connection identity)"
        string browserId "random uid from client (sticky per tab)"
        string serverId "FK to Server (nullable)"
        string url "current page URL"
        string title "current page title"
        number connectedAt "timestamp"
        number nextId "command sequence counter"
    }

    MCP_SESSION {
        string sessionId "per-transport"
        string toolset "core (9) | full (23)"
        string currentProject "resolved project dir (nullable)"
    }

    CDP_RELAY {
        boolean debuggingActive "debugger attached?"
        boolean debuggingRequested "request sent?"
        number idleTimeoutMs "5min default"
    }

    AVAILABLE_TAB {
        number tabId "Chrome tab ID"
        string url "page URL"
        string serverId "associated server"
        string projectId "associated project"
    }

    PLAYWRIGHT_CLIENT {
        string clientId "incrementing int"
        boolean subscribedToAll "Target.setAutoAttach called?"
    }
```

### Identity Schemes

| Entity | Identity | Generated by | Lifetime |
|--------|----------|--------------|----------|
| Project | `basename-hash4` from absolute dir path | `makeProjectId(directory)` | Persistent (directory exists) |
| Server | PID string | `makeServerId(pid)` | Ephemeral (process lifetime) |
| Browser | `connId` (random, per WS) + `browserId` (random, per sessionStorage) | Client: `connId` at WS open, `browserId` from `sessionStorage.__web_dev_mcp_browser_id__` | Connection lifetime / tab lifetime |
| MCP Session | Transport sessionId | MCP SDK | SSE connection lifetime |

### Registry Indices

The `ServerRegistry` maintains three indices for fast lookup:

```mermaid
graph LR
    subgraph ServerRegistry
        SERVERS["servers<br/>Map&lt;serverId, RegisteredServer&gt;"]
        DIRPORT["directoryPortIndex<br/>Map&lt;'dir:port', serverId&gt;"]
        PROJID["projectIdIndex<br/>Map&lt;projectId, serverId&gt;"]
        ORDER["connectionOrder<br/>string[] (append-only)"]
    end

    SERVERS --- DIRPORT
    SERVERS --- PROJID
    SERVERS --- ORDER
```

- `directoryPortIndex` prevents duplicate registrations for the same dir+port (re-registration replaces)
- `projectIdIndex` enables lookup by short ID (e.g. `vite-app-a3f7`)
- `connectionOrder` tracks registration order for "latest server" semantics

---

## Communication Protocols

```mermaid
graph LR
    subgraph "Dev Servers (Vite, Next.js, Storybook)"
        ADAPTER["Framework Adapter"]
    end

    subgraph "Gateway :3333"
        REG["/__gateway/register<br/>POST (HTTP)"]
        UNREG["/__gateway/unregister/:id<br/>POST (HTTP)"]
        EVENTS_WS["/__events<br/>WS (browser → gateway)"]
        DEV_WS["/__dev-events<br/>WS (adapter → gateway)"]
        RPC_WS["/__rpc<br/>WS (bidirectional)"]
        ADMIN_API["/__admin/api<br/>GET (HTTP)"]
        ADMIN_SSE["/__admin/events<br/>GET (SSE)"]
        ADMIN_LOGS["/__admin/logs<br/>GET (HTTP)"]
        ADMIN_EVAL["/__admin/eval<br/>POST (HTTP)"]
        MCP_SSE["/__mcp/sse<br/>GET (SSE)"]
        MCP_MSG["/__mcp/message<br/>POST (HTTP)"]
        CDP_EXT["/__cdp-extension<br/>WS"]
        CDP_PW["/devtools/browser/*<br/>WS"]
        CDP_HTTP["/json/version, /json/list<br/>GET (HTTP)"]
        CLIENT_JS["/__web-dev-mcp.js<br/>GET (HTTP)"]
    end

    subgraph "Browser Pages"
        CLIENT["web-dev-mcp-client.js"]
    end

    subgraph "Admin UI"
        ADMIN["Admin Dashboard"]
    end

    subgraph "AI Agents"
        AGENT["Claude Code / Cursor / etc."]
    end

    subgraph "Chrome Extension"
        EXT["CDP Bridge"]
    end

    subgraph "Playwright"
        PW["@xmorse/playwright-core"]
    end

    %% Adapter registration
    ADAPTER -->|"register on startup"| REG
    ADAPTER -->|"unregister on shutdown"| UNREG
    ADAPTER -->|"build/HMR events"| DEV_WS

    %% Browser connections
    CLIENT -->|"console, errors, network"| EVENTS_WS
    RPC_WS <-->|"commands + responses"| CLIENT
    CLIENT_JS -.->|"injected into pages"| CLIENT

    %% Admin connections
    ADMIN -->|"poll state"| ADMIN_API
    ADMIN -->|"stream events"| ADMIN_SSE
    ADMIN -->|"query logs"| ADMIN_LOGS
    ADMIN -->|"run JS"| ADMIN_EVAL

    %% Agent connections
    AGENT -->|"SSE connect"| MCP_SSE
    AGENT -->|"tool calls"| MCP_MSG

    %% CDP connections
    EXT <-->|"CDP messages"| CDP_EXT
    PW <-->|"connectOverCDP"| CDP_PW
    PW -->|"discovery"| CDP_HTTP
```

### Protocol Details

| Connection | Transport | Direction | Message Format |
|-----------|-----------|-----------|----------------|
| Adapter → Register | HTTP POST | Adapter → Gateway | `{ type, port, pid, directory, rpcEndpoint?, mcpEndpoint? }` |
| Browser → Events | WebSocket | Browser → Gateway | `{ channel, payload, browserId }` |
| Browser ↔ RPC | WebSocket | Bidirectional | Gateway→Browser: `{ id, method, params }`, Browser→Gateway: `{ id, result }` or `{ id, error }` or `{ type: 'init', browserId, url, title }` |
| Adapter → Dev Events | WebSocket | Adapter → Gateway | `BuildEventPayload` (build:start, build:update, build:error, build:complete) |
| Admin → API | HTTP GET | Admin → Gateway | Response: `{ uptime_ms, mode, browsers[], servers[], mcp_sessions }` |
| Admin ← SSE | SSE | Gateway → Admin | Events: `browser_connect`, `browser_init`, `browser_disconnect`, `log` |
| Agent ↔ MCP | SSE + HTTP POST | Bidirectional | MCP protocol (JSON-RPC over SSE transport) |
| Extension ↔ CDP | WebSocket | Bidirectional | `tabAvailable`, `tabUnavailable`, `requestDebug`, `releaseDebug`, `forwardCDPEvent`, `forwardCDPCommand` |
| Playwright ↔ CDP | WebSocket | Bidirectional | Raw CDP protocol (proxied through relay) |

---

## Server Registration Lifecycle

```mermaid
sequenceDiagram
    participant Adapter as Framework Adapter
    participant Gateway
    participant Registry as ServerRegistry
    participant Writers as Log Writers
    participant FS as Filesystem

    Note over Adapter: Dev server starts (vite dev, next dev, etc.)

    Adapter->>Gateway: POST /__gateway/register<br/>{type, port, pid, directory}
    Gateway->>Registry: makeProjectId(directory) → "app-a3f7"
    Gateway->>Registry: makeServerId(pid) → "12345"

    alt directory:port already registered
        Registry->>Registry: remove old server (re-registration)
    end

    Gateway->>FS: mkdirSync(directory/.web-dev-mcp/)
    Gateway->>FS: Create empty .ndjson files<br/>(console, errors, dev-events, server-console)
    Gateway->>Writers: Create per-project writers<br/>(ConsoleWriter, ErrorsWriter, DevEventsWriter, ServerConsoleWriter)
    Gateway->>Registry: registry.add(server)
    Gateway-->>Adapter: { serverId, projectId, logDir, gatewayMcpUrl, gatewayRpcUrl }

    Note over Adapter: Adapter injects client script into pages

    Adapter->>Gateway: WS /__dev-events?server=12345
    Note over Adapter,Gateway: Build/HMR events flow over this WS

    loop Every 5 seconds (heartbeat)
        Gateway->>Gateway: registry.cleanupDeadServers()
        Gateway->>Gateway: process.kill(pid, 0) — check alive
        alt PID dead (grace: 30s after register)
            Gateway->>Registry: registry.remove(serverId)
            Gateway->>Gateway: removeBrowsersByServer(serverId)
            Gateway->>Writers: projectWriters.delete(directory)
        end
        Gateway->>Gateway: evictOrphanBrowsers(registeredIds, grace: 15s)
    end

    Note over Adapter: Dev server stops

    Adapter->>Gateway: POST /__gateway/unregister/12345
    Gateway->>Registry: registry.remove("12345")
    Gateway->>Gateway: removeBrowsersByServer("12345")
    Gateway->>Writers: projectWriters.delete(directory)
```

---

## Browser Connection Lifecycle

```mermaid
sequenceDiagram
    participant Page as Browser Page
    participant Client as web-dev-mcp-client.js
    participant EventsWS as /__events WS
    participant RpcWS as /__rpc WS
    participant Gateway
    participant Admin as Admin SSE

    Note over Page: Page loads, adapter injected <script src="/__web-dev-mcp.js">

    Client->>Client: browserId = sessionStorage.__web_dev_mcp_browser_id__<br/>or generate new random uid
    Client->>Client: Determine gateway origin<br/>(window.__WEB_DEV_MCP_ORIGIN__ or location.origin)

    par Open both WebSockets
        Client->>EventsWS: WS connect /__events?server=<serverId>
        Client->>RpcWS: WS connect /__rpc?server=<serverId>
    end

    Gateway->>Gateway: Create BrowserConnection<br/>{connId: random, serverId: from ?server=, browserId: null}
    Gateway->>Admin: SSE: browser_connect {connId, serverId}

    Client->>RpcWS: { type: 'init', browserId, url, title }
    Gateway->>Gateway: conn.browserId = browserId<br/>conn.url = url, conn.title = title
    Gateway->>Admin: SSE: browser_init {connId, browserId, serverId, url, title}

    par Console/Error/Network patching
        Client->>Client: Patch console.log/warn/error/info/debug
        Client->>Client: Patch window.onerror, onunhandledrejection
        Client->>Client: Patch fetch(), XMLHttpRequest
    end

    loop Browser events
        Page->>Client: console.log("hello")
        Client->>EventsWS: { channel: 'console', payload: {level:'log', args:['hello']}, browserId }
        Gateway->>Gateway: Write to per-project ConsoleWriter (NDJSON)
        Gateway->>Admin: SSE: log {channel:'console', payload, browserId}
    end

    loop Gateway commands
        Gateway->>RpcWS: { id: '1', method: 'eval', params: {code: '...'} }
        Client->>Client: Execute code in page context
        Client->>RpcWS: { id: '1', result: '...' }
    end

    Note over Page: Page unloads or tab closes

    Client--xRpcWS: WS close
    Gateway->>Gateway: Reject all pending commands
    Gateway->>Gateway: Remove from browsers Map + connectionOrder
    Gateway->>Admin: SSE: browser_disconnect {connId, browserId, serverId}

    Note over Gateway: If serverId not in registry after 15s grace → evict
```

### Orphan Browser Handling

Browsers are "orphans" when their `serverId` doesn't match any registered server. This happens when:
- A dev server crashes without unregistering
- Gateway restarts but servers haven't re-registered yet
- Browser tabs from old dev sessions reconnect

Rules:
- Browsers with **no serverId** (untagged) are never evicted — they're valid gateway-level connections
- Browsers with a **stale serverId** get evicted after 15-second grace period
- Grace period prevents false evictions during gateway restart (servers re-register within seconds)

---

## Log Data Flow

```mermaid
graph TB
    subgraph "Browser Page"
        CONSOLE["console.log/warn/error"]
        FETCH["fetch() / XHR"]
        ERRORS["unhandled exceptions<br/>+ rejections"]
    end

    subgraph "Client Script"
        PATCH["Patched APIs<br/>(intercepts all calls)"]
        QUEUE["Event queue<br/>(max 1000 pending)"]
    end

    subgraph "Gateway"
        EVENTS["/__events WS"]
        ROUTER["Writer Router<br/>(project writers || gateway writers)"]
    end

    subgraph "Writers (per project)"
        CW["ConsoleWriter<br/>console.ndjson"]
        EW["ErrorsWriter<br/>errors.ndjson"]
        NW["NetworkWriter<br/>network.ndjson<br/>(optional, buffered 100ms)"]
        SCW["ServerConsoleWriter<br/>server-console.ndjson"]
    end

    subgraph "Dev Events (separate WS)"
        DEV_ADAPTER["Framework Adapter"]
        DEV_WS["/__dev-events WS"]
        DEW["DevEventsWriter<br/>dev-events.ndjson"]
    end

    subgraph "Log Storage"
        NDJSON["NDJSON files<br/>(auto-rotate at size limit)"]
    end

    subgraph "Consumers"
        ADMIN_SSE["Admin SSE<br/>(real-time broadcast)"]
        LOG_READER["Log Reader<br/>(query engine)"]
        MCP_DIAG["get_diagnostics<br/>(MCP tool)"]
        MCP_NOTIF["MCP notifications<br/>(on errors)"]
    end

    CONSOLE --> PATCH
    FETCH --> PATCH
    ERRORS --> PATCH
    PATCH --> QUEUE
    QUEUE -->|"WS reconnect: 2s backoff"| EVENTS

    EVENTS --> ROUTER
    ROUTER --> CW & EW & NW & SCW
    CW & EW & NW & SCW --> NDJSON

    DEV_ADAPTER -->|"build:start/update/error/complete"| DEV_WS
    DEV_WS --> DEW
    DEW --> NDJSON

    EVENTS -->|"broadcastToAdmin()"| ADMIN_SSE
    NDJSON -->|"queryLogs()"| LOG_READER
    LOG_READER --> MCP_DIAG

    EW -->|"on error"| MCP_NOTIF
    SCW -->|"on server error"| MCP_NOTIF
    DEW -->|"on build:error"| MCP_NOTIF
```

### NDJSON Event Format

Every log entry on disk:

```json
{"id": 1, "ts": 1713400000000, "channel": "console", "payload": {"level": "log", "args": ["hello world"]}}
```

| Field | Type | Description |
|-------|------|-------------|
| `id` | number | Monotonic per writer (resets on rotation) |
| `ts` | number | `Date.now()` at write time |
| `channel` | string | `console`, `errors`, `dev-events`, `server-console`, `network` |
| `payload` | object | Channel-specific (see below) |

### Payload Shapes

| Channel | Payload Type | Fields |
|---------|-------------|--------|
| `console` | ConsolePayload | `level` (log/warn/error/info/debug), `args` (string[], truncated at 2000 chars), `stack?` |
| `errors` | ErrorPayload | `type` (console-error/unhandled-exception/unhandled-rejection), `message`, `stack?`, `file?`, `line?` |
| `network` | NetworkPayload | `method`, `url`, `status`, `duration`, `initiator` (fetch/xhr) |
| `server-console` | ServerConsolePayload | `level`, `args`, `source: 'server'`, `stack?` |
| `dev-events` | BuildEventPayload | `type` (build:start/update/error/complete), `error?`, `modules?` |

### Log Query Engine

`queryLogs()` reads NDJSON files with filtering:

```
queryLogs(files, { channel?, sinceId?, limit (max 200)?, level?, search?, browserId? })
→ { events[], total, returned, next_cursor }
```

`getDiagnostics()` queries all 4 channels + build status → `DiagnosticsResult`:
```
{ build: BuildStatus, logs: { console, errors, network, server_console }, summary: DiagnosticSummary, checkpoint_ts }
```

### Writer Rotation

Writers auto-rotate NDJSON files at a configurable size limit (`maxFileSizeMb`, default unset). Rotation scheme: `.ndjson` → `.1.ndjson` → `.2.ndjson` → `.3.ndjson` (max 3 rotated files).

### SSE Replay Buffer

Admin SSE has a 1MB in-memory replay buffer for `Last-Event-ID` reconnection:
- Each event gets monotonic `id` (sseSeq)
- Buffer evicts entries all connected clients have received
- Hard cap at 1MB regardless of client state (protects against slow/stale clients)
- Events: `connected`, `browser_connect`, `browser_init`, `browser_disconnect`, `log`

---

## Current Admin UI (to be replaced)

The existing admin at `examples/admin-svelte/` consumes these gateway endpoints:

| What it does | Endpoint | Transport |
|-------------|----------|-----------|
| Load full state (projects, servers, browsers) | `GET /__admin/api` | HTTP |
| Stream live events | `GET /__admin/events` | SSE |
| Query historical logs | `GET /__admin/logs` | HTTP |
| Execute JS in browser | `POST /__admin/eval` | HTTP |

### Known Issues

1. **Log-centric** — main view is a log stream, not a system overview
2. **Confusing sidebar** — hash suffixes on project IDs (`app-a3f7`), ports everywhere
3. **Orphan browsers** — show up in `__unknown` section with no explanation
4. **Clear doesn't persist** — UI clears local state, but server still has logs (no `POST /__admin/logs/clear` endpoint)
5. **No server lifecycle SSE events** — admin only learns about server changes via polling `/__admin/api`
6. **SSE 6-connection limit** — browsers have a hard limit on concurrent SSE connections per domain, problematic when running multiple apps
